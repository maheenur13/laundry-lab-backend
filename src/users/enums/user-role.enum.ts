/**
 * User roles for role-based access control.
 * These values are stored in the database and used by the mobile app.
 */
export enum UserRole {
  CUSTOMER = 'customer',
  DELIVERY = 'delivery',
  ADMIN = 'admin',
}
